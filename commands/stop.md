| description | disable-model-invocation |
|---|---|
| Stop the observability dashboard | false |

Stop the Multi-Agent Observability dashboard by running the reset script.

Use the Bash tool to run:
```
${CLAUDE_PLUGIN_ROOT}/scripts/reset-system.sh
```

After stopping, confirm to the user that the server and client processes have been terminated.
